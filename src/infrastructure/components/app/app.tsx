import { Layout } from '../layout/layout';
import { AppRoutes } from '../routes/routes';

export function App() {
    return (
        <Layout>
            <>
                {' '}
                <p>cuenta</p>
                <AppRoutes />
            </>
        </Layout>
    );
}
